server:
  port: 9002


spring:
  application:
    name: USER-SERVICE
  h2:
    console:
      enabled=true:
#  datasource:
#    url=jdbc:h2:mem:testdb:
#    driverClassName=org:h2:Driver:
#    username=sa:
#    password=password:
#
#  jpa:
#    database-platform=org:hibernate:dialect:H2Dialect:eureka:

  client:
    register-with-eureka: true
    fetch-registry: true
    service-url:
      default-zone: http://localhost:8761/eureka/
#  instance:
#    hostname: localhost

